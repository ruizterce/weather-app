import initWeatherApp from './DOMHandler';
import './style.css';

//Select the desired container and initialize the weather app
const container = document.querySelector('.container');
initWeatherApp(container);
